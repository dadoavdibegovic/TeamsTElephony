# URL of the app-backend (where IncomingCall events get forwarded)
BACKEND_URL=http://localhost:3000

# Same ACS connection string as app-backend — used here only if the function
# itself needs to call ACS; currently the function only forwards to backend.
ACS_CONNECTION_STRING=endpoint=https://<acs>.communication.azure.com/;accesskey=...
